class FeatureData {
  constructor({ icon, altText, title, description }) {
    this.icon = icon
    this.iconAlternativeText = altText
    this.title = title
    this.description = description
  }
}

export default FeatureData
